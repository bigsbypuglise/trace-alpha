# Build outputs
build/
# Side build trees, e.g. configuring without the vcpkg toolchain to prove the
# TRACE_WITH_FFMPEG / TRACE_WITH_AUDIO guards still compile with those absent.
build-*/
out/
cmake-build-*/

# CMake
CMakeCache.txt
CMakeFiles/
Makefile
cmake_install.cmake
compile_commands.json

# OS/editor
.DS_Store
.vscode/
.idea/
*.swp

# Icon working files (superseded revisions, source zips)
assets/icons/_old/
assets/icons/*.zip

# Interface design working files (source zips)
assets/Interface/*.zip
